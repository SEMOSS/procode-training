{
    "private": true,
    "scripts": {
        "fix": "prettier . --write && eslint --fix; pre-commit run --all-files || pre-commit run --all-files",
        "dev": "pnpm --dir client dev",
        "javadoc": "mvn clean javadoc:javadoc; mvn site; python -m http.server 1227 -d target/site/apidocs/"
    },
    "devDependencies": {
        "eslint": "^9.36.0",
        "eslint-config-prettier": "^10.1.8",
        "husky": "^9.1.7",
        "lint-staged": "^16.2.3",
        "prettier": "^3.6.2",
        "typescript-eslint": "^8.44.1"
    },
    "lint-staged": {
        "**/*.{js,jsx,ts,tsx,json,html,css}": [
            "prettier --write",
            "eslint"
        ]
    }
}
